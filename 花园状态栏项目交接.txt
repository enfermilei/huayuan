花园状态栏项目交接
更新时间：2026-07-25
当前 GitHub HEAD（已推送）：ac942d5
本地另有未推送改动：背包格子 / 立绘锁定 / 进度条修正 / 在场拖动 / Header 微动效等
→ 发卡前请先 pnpm build、提交并推送，再用新 commit 钉正则（勿再用 2cea4bf / @main）

================================================================================
0. 新会话 60 秒上手
================================================================================

1. git pull；看 HEAD；有未推送 UI 先 build + commit + push，再钉新 commit
2. 本地：pnpm watch + pnpm serve:cors；正则「测试」→
   http://127.0.0.1:5500/dist/花园/界面/状态栏/index.html
3. 启用脚本：花园 MVU、花园变量结构；关「mvu美化」CDN 正则防冲突
4. 酒馆助手 → 实时监听 → 允许监听
5. 验：顶部胶囊动效、在场卡横滑/拖动、好感条≈数字%、安全模式、锁定立绘、底栏背包
6. 发卡永远钉 @commit，禁止 @main（见 §8）


================================================================================
1. 项目是什么
================================================================================

基于酒馆助手模板，给角色卡「在满是数删的神秘组织里当主人」做的 MVU 状态栏前端。

项              内容
--------------  --------------------------------------------------------------
本地仓库        e:\tavern_helper_template-main
业务代码        src/花园/
GitHub          https://github.com/enfermilei/huayuan（origin）
技术栈          Vue 3 + Pinia + Zod 4 + Tailwind(可用) + 酒馆助手 API + MVU
运行形态        消息楼层 iframe 前端（禁止再把核心逻辑塞回正则 inline script）
设定方向        花园组织经营 / 成员管理 / 赌场·酒馆等
展示内容        世界氛围条、主角、组织、在场成员(立绘)、名册、地图、背包、设置


================================================================================
2. 关键背景（为何是 iframe）
================================================================================

旧方案：整份 HTML（含 <script>）进酒馆正则。
根因：SillyTavern innerHTML 注入时 <script> 不执行 → 界面停在 -- / 同步中...
验证：DOM 里有 init 源码字符串，但 typeof init === 'undefined'。

现方案：酒馆助手 iframe；视觉在 CSS/Vue，逻辑在 TS。
正则只做占位 → $('body').load(状态栏 URL)。


================================================================================
3. 目录结构（状态栏相关）
================================================================================

src/花园/
  schema.ts / schema.json         # MVU zod；build/watch 生成 json
  世界书/index.yaml
  脚本/
    MVU/index.ts                  # 包装 MagVarUpdate（先绑 Vue 全局常量）
    变量结构/index.ts             # registerMvuSchema(Schema)
  界面/状态栏/
    index.html / index.ts         # 启动占位 → wait Mvu/stat_data(120s) → Pinia → 自定义立绘 init → mount
    App.vue                       # 布局 + 弹层（名册/地图/背包/设置/身份卡）
    store.ts                      # defineMvuDataStore 楼层双向同步
    settings.ts                   # UI 设置 localStorage：想法/动画/名册排序/安全模式
    portrait.ts                   # 立绘规范化、R18 判定、URL 拼装
    portraitLocks.ts              # 立绘锁定（localStorage；忽略后续 MVU 立绘状态）
    customPortraits.ts            # 自定义立绘 IndexedDB + 压缩
    inventory.ts                  # 背包解析/分类/重要标记
    worldAtmosphere.ts            # 天气/时段/温度/Header 氛围分类（纯 CSS 图标，无 emoji）
    utils.ts                      # 金额、toPercent、resolvePortrait 等
    mapData.ts / customLandmarks.ts
    global.css
    components/
      WorldHeader                 # 日期/时间/星期/天气/温度 + 微动效
      UserPanel                   # 主人档案（已不含背包）
      OrgPanel / CharacterList    # 在场：滚轮横滑 + 按住拖动
      ActionBar                   # 成员名册 | 组织地图 | 背包 | 设置
      RosterPanel / MapPanel / SettingsPanel / IdentityCard
      InventoryGrid / InventoryPanel
      LandmarkGlyph

portraits/{角色名}/*.png          # 832×1216；天音纯 / 天野神乐 / 夏语冰 等
scripts/cors-static-server.js     # pnpm serve:cors → :5500
dist/花园/...                     # 打包产物（[bot] bundle 可能覆盖）


================================================================================
4. 已完成功能（按阶段）
================================================================================

阶段  内容
----  ----------------------------------------------------------------------
0-2   正则 script 根因确认；Vue iframe；Schema + defineMvuDataStore
3-4   名册 / 地图初版
5     设置：想法摘要、入场动画、名册优先在场（garden-mvu:ui-settings）
6-7   立绘匹配 + 发布 huayuan + testingcf.jsDelivr
8-10  白屏修复（pinia@3.0.4、Vue 全局常量、MVU 包装）；子目录 URL；启动超时
11-12 立绘资源补齐；在场 832×1216 contain，忠诚栏不被裁
13    设置改名「设置」；去掉立绘基址/扩展名 UI；CDN 基址写死
14    安全模式（默认关）：隐藏性事/裸体 → 日常-普通
15    自定义立绘：IndexedDB；设置面板管理；身份卡上传/替换
16    好感/忠诚进度条：按 0~100 与数字一致（不再 (v+100)/2）
17    背包格子 UI：分类、重要标「重」、点击看描述；底栏「背包」大面板
      （主人档案内嵌背包已移除，避免竖滚）
18    立绘锁定：身份卡锁定当前态；变量再变仍显示锁定图；设置可批量解锁
19    在场成员：滚轮横滑 + 左键拖动（向左拖→向右滚）；拖动不误点开卡
20    WorldHeader 微动效 + 按天气/时段字符串切换纯 CSS 图标；深夜压暗 Header

本地 CORS：必须 pnpm serve:cors（勿用无 CORS 的 Live Server 占 5500）


================================================================================
5. Schema 摘要
================================================================================

stat_data：
  系统      日期/时间/星期/天气/温度；组织全局；金缕赌场；迷迭香酒馆
            （UI 可选读 系统.时段；无则用 时间 推断氛围）
  成员名册  职务、位置、好感、忠诚、目标、身体、着装、怀孕、立绘、在场、背包…
  主角      职务、位置、贡献、资金、着装、背包

注意：
- 好感/忠诚：schema clamp -100~100；进度条 UI 按 abs 映射到 0~100%
- 是否在场：z.boolean()；UI isPresent 兼容 'true'/1
- 立绘：主类型 日常|服装|性事；次类型按主类型校验；差分 1|2
- 背包物品：{ 描述, 数量 }；数量≤0 会被 schema transform 剔掉


================================================================================
6. 立绘约定
================================================================================

路径：
  portraits/{角色名}/{角色名}-{主类型}-{次类型}-{差分}.png
例：portraits/天音纯/天音纯-日常-普通-1.png

resolvePortrait 优先级：
  1) 本机锁定立绘（portraitLocks）
  2) 安全模式且 R18 → 日常-普通-1
  3) 自定义立绘 IndexedDB（customPortraits）
  4) CDN DEFAULT_PORTRAIT_BASE/.../stem.png
  5) placehold

默认 CDN 基址（代码写死，设置里不再改）：
  https://testingcf.jsdelivr.net/gh/enfermilei/huayuan@main/portraits

R18（安全模式隐藏）：主类型=性事；或 服装+裸体

锁定：
  localStorage key: garden-mvu:portrait-locks
  入口：身份卡「锁定立绘」/「解锁立绘」；设置里可清除

在场卡布局：宽约 280px；立绘区 aspect-ratio 832/1216；object-fit:contain；
给 card-body 留高，避免裁切好感/忠诚栏。


================================================================================
7. 背包约定
================================================================================

- 格子展示 + 自动分类（文件/钥匙/消耗/装备/贵重/其他）
- 重要物品角标「重」（关键词：机密/合同/钥匙…）
- 点击格子展开完整描述（无复制按钮）
- 入口：底栏「背包」（主角）；身份卡/名册随身页（成员，compact）
- 主人档案不再内嵌背包


================================================================================
8. 设置项（garden-mvu:ui-settings）
================================================================================

- 显示内心想法摘要
- 入场动画
- 名册优先在场
- 安全模式（默认 false）
- 自定义立绘管理 / 立绘锁定列表
- 「恢复默认」只重置上述 UI 开关，不清 IndexedDB 立绘、不清锁定（锁定在设置区单独清）

已移除：立绘资源基址、立绘扩展名（避免用户填坏导致全 placehold）


================================================================================
9. 本地开发 vs 发卡
================================================================================

【本地】
  pnpm watch
  pnpm serve:cors
  酒馆 http://127.0.0.1:1234/
  实时监听开；脚本：花园 MVU + 花园变量结构
  正则「测试」→ http://127.0.0.1:5500/dist/花园/界面/状态栏/index.html
  关 CDN「mvu美化」防双开

【发卡】
  镜像：testingcf.jsdelivr.net（勿 raw.githubusercontent.com）
  必须钉 commit，禁止 @main

  已推送可参考（含安全模式/自定义立绘；不含其后本地未推送项）：
    状态栏：
      https://testingcf.jsdelivr.net/gh/enfermilei/huayuan@ac942d5/dist/花园/界面/状态栏/index.html
    MVU：
      import 'https://testingcf.jsdelivr.net/gh/enfermilei/huayuan@ac942d5/dist/花园/脚本/MVU/index.js';

  立绘资源根（可用 @main，单图缓存影响小）：
    https://testingcf.jsdelivr.net/gh/enfermilei/huayuan@main/portraits

正则示例：
```
<body>
  <script>
    $('body').load(
      'https://testingcf.jsdelivr.net/gh/enfermilei/huayuan@ac942d5/dist/花园/界面/状态栏/index.html',
    );
  </script>
</body>
```
推送新功能后，把上面 @ac942d5 换成新 commit。


================================================================================
10. 关键事故（务必读）
================================================================================

A. pinia@4 白屏
   unpinned pinia/+esm → 缺 __VUE_PROD_DEVTOOLS__
   已修：webpack 钉 pinia@3.0.4；index.html 注入全局常量；MVU 包装脚本
   禁止角色卡裸 import MagVarUpdate/artifact/bundle.js

B. @main 缓存坏包
   jsDelivr @main 可能仍是旧/坏包 → 永远钉 @commit

C. [bot] bundle 覆盖 dist
   合并后核对状态栏包；UI 修复后重新钉新 commit


================================================================================
11. 后续目标
================================================================================

P0
  [ ] 把本地未推送改动 build + push，卡内正则钉新 commit
  [ ] Schema 非法/缺字段时 UI 不刷新的防护与提示
  [ ] jsDelivr 长期钉 tag/commit；少用 @main

P1
  [ ] 名册筛选/排序/危险置顶
  [ ] 地图 → 可点宅邸平面图
  [ ] 设置/锁定/自定义立绘可同步到角色卡或脚本变量（现仅本机）

P2
  [ ] 名册可操作、地图可探索
P3
  [ ] 主题/紧凑模式/经营摘要/关系网


================================================================================
12. 已知坑
================================================================================

问题                    说明
----------------------  ------------------------------------------------------
CORS                    本地必须 pnpm serve:cors
pinia CDN               禁止 unpinned pinia/+esm
MagVarUpdate 裸 import  用仓库包装 MVU index.js
jsDelivr @main          发卡禁用；立绘资源根可用
[bot] bundle            可能冲 dist
defineMvuDataStore      parse 失败会跳过同步
立绘路径                必须含角色子目录
进度条                  已改为与数字 0~100 一致；勿再改回 (v+100)/2 除非产品要「0=半条」
安全模式 vs 锁定        锁定优先取锁定态，再套安全模式（锁定 R18 仍可能被安全模式盖住）
自定义/锁定             本机存储，换浏览器会丢
主人档案                已无背包；背包走底栏
双正则                  「测试」与「mvu美化」只开一个
async-wait-until        状态栏显式 120s；默认库超时仅 5s


================================================================================
13. 关键 commit
================================================================================

ef4369c   MVU 包装 + 全局常量
9e333c3   立绘子目录 + 启动占位 + 空基址回退
9865be3   天音纯更新；天野神乐/夏语冰立绘
2cea4bf   在场立绘 832×1216，避免裁忠诚栏
eb3b21a   合并 bot bundle
9b01471   安全模式 + 自定义立绘 + 设置改名
ac942d5   合并远程后推送点（当前 GitHub 可钉）
（其后本地：背包格子、立绘锁定、进度条、拖动、Header 氛围 — 待推送）


================================================================================
14. localStorage / IndexedDB 一览
================================================================================

garden-mvu:ui-settings      设置开关（Pinia + useLocalStorage）
garden-mvu:portrait-locks   立绘锁定表
IndexedDB garden-mvu-portraits / images   自定义立绘 Blob
